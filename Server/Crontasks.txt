
This task (every day at 3 a.m) changes the color space of all images to RGB to avoid problems in Android:
sudo -- sh -c 'echo "0 3 * * * root for f in *.(png|jpg|jpeg|PNG|JPG|JPEG); do convert \$f -colorspace RGB \$f; done" >> /etc/crontab'